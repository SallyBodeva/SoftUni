function change(input){
    const change = Number(input[0]);
    let neededCoins = 0;
    while(change > 0){
        if(change / 2 > 0){
            neededCoins += Math.floor(change / 2);
            change /= 2;
        } else if(change / 1){

        }
    }

}